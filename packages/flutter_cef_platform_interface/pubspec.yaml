name: flutter_cef_platform_interface
description: "The common platform interface for flutter_cef: the shared Dart types and method-channel contract each platform implementation (macOS, future Windows/Linux) speaks."
version: 0.1.3
homepage: https://github.com/FlutterFlow/flutter_cef
repository: https://github.com/FlutterFlow/flutter_cef
issue_tracker: https://github.com/FlutterFlow/flutter_cef/issues
topics:
  - webview
  - chromium
  - cef
  - browser
# NOTE: We strongly prefer a constraint that allows the current version and
# future patch/minor releases of the interface so federated implementations
# stay compatible. Breaking changes to this package require a major bump and a
# coordinated update of all implementations.

environment:
  sdk: ^3.4.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
